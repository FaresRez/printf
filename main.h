#ifndef _MAIN_H
#define _MAIN_H
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
int _printf(const char *format, ...);
void print_int(int num);
int len(char *ch);
#endif
